name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-20.04
    steps:
      - name: echo a string
        run: echo "Hello GitHub Actions!"
      - name: multiline scripts
        run: |
          node -v
          npm -v
      - name: failing command
        run: eccho "Some message never displayed"
